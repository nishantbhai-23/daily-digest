triage_agent/calendar_agent/notes_agent.build_map_system_prompt(use_persona=True/False) — see each agent module for the exact rendered text; not snapshotted as one string here since three separate prompts (one per source) are being compared as a single variant.